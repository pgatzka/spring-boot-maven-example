name: example_dev

services:
  postgres:
    image: 'postgres:18-alpine'
    environment:
      POSTGRES_DB: example_dev
      POSTGRES_USER: example_dev
      POSTGRES_PASSWORD: example_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data: