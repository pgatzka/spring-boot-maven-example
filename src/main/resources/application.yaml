spring:
  application:
    name: example
  main:
    banner-mode: off
  jpa:
    hibernate:
      ddl-auto: validate
logging:
  level:
    root: info
    io.github.pgatzka.example: info
    org.springframework.boot: warn